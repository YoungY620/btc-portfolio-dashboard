# Copy this file to `.env.local` for local development.
#
# This dashboard no longer uses Binance or OKX account API keys.
# Strategy settings and public market-data URLs live in config/dashboard.json.
# Manual balances are stored in browser localStorage.
